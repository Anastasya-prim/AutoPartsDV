# Обычно не нужен: в образе по умолчанию NEXT_PUBLIC_API_URL=/api (тот же хост, что сайт — Nginx → API).
#
# Укажите только если фронт и API на разных доменах, например:
# NEXT_PUBLIC_API_URL=https://api.autoparts.example.ru/api

# NEXT_PUBLIC_API_URL=/api
