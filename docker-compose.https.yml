# Подключение: docker compose -f docker-compose.yml -f docker-compose.https.yml up -d
# Нужны сертификаты в /etc/letsencrypt на хосте (certbot).
services:
  nginx:
    ports:
      - "443:443"
    volumes:
      - /etc/letsencrypt:/etc/letsencrypt:ro
